function solve(first, second, third){
    console.log(first.length + second.length + third.length);
    console.log(Math.floor((first.length + second.length + third.length) / 3));
}
solve('chocolate', 'ice cream', 'cake')